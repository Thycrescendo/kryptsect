'use client';
import { useState } from 'react';
export default function TradePanel({ title = 'Spot Trade' }: { title?: string }) {
  const [side, setSide] = useState<'buy'|'sell'>('buy');
  return <div className="rounded-2xl border border-white/10 bg-krypt-panel p-4"><h2 className="mb-4 font-bold">{title}</h2><div className="mb-4 grid grid-cols-2 gap-2"><button onClick={() => setSide('buy')} className={side === 'buy' ? 'rounded-lg bg-krypt-green py-2 font-bold text-black' : 'rounded-lg bg-white/10 py-2 font-bold'}>Buy</button><button onClick={() => setSide('sell')} className={side === 'sell' ? 'rounded-lg bg-krypt-red py-2 font-bold text-black' : 'rounded-lg bg-white/10 py-2 font-bold'}>Sell</button></div><select className="mb-3 w-full rounded-xl border border-white/10 bg-krypt-bg p-3"><option>Limit</option><option>Market</option><option>Stop Limit</option></select><input placeholder="Price USDT" className="mb-3 w-full rounded-xl border border-white/10 bg-krypt-bg p-3 outline-none focus:border-krypt-gold" /><input placeholder="Amount BTC" className="mb-3 w-full rounded-xl border border-white/10 bg-krypt-bg p-3 outline-none focus:border-krypt-gold" /><div className="mb-4 flex justify-between text-sm text-gray-400"><span>Available</span><span>0.00 USDT</span></div><button className={side === 'buy' ? 'w-full rounded-xl bg-krypt-green py-3 font-black text-black' : 'w-full rounded-xl bg-krypt-red py-3 font-black text-black'}>Connect Wallet to {side === 'buy' ? 'Buy' : 'Sell'}</button></div>;
}
